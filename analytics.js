/**
 * Vercel Web Analytics Initialization
 * Using @vercel/analytics package with inject function for static HTML
 */
import { inject } from '@vercel/analytics';

// Initialize Vercel Analytics
inject();
